package com.mechanitis.sudoku.data;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ValidatorTest {
    @Test
    private void each_row_should_only_have_the_numbers_1_9() {
        fail("Not implemented");
    }

    // each row can have each number once only
    // each column should only have the numbers 1-9
    // each column can have each number once only
    // each block can have each number once only
    // each number (1-9) should appear 9 times in the grid

}